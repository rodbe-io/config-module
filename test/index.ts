import { Config } from '../src';

// Config.init({ filePath: '../taks/config/config.json' });
Config.init({ filePath: `test/config.local.yml` });
Config.init({ env: 'prod' });
Config.init({ env: 'prod' });
